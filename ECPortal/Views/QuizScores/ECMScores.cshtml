@* @model List<Pk.Com.Jazz.ECP.Models.QuizScores>
@using Newtonsoft.Json;

@{
    ViewData["Title"] = "Employee Quiz Scores";

    var quizStatusData = Model != null
        ? JsonConvert.SerializeObject(Model.GroupBy(q => q.Status).Select(g => new { Status = g.Key, Count = g.Count() }).ToList(), new JsonSerializerSettings { ReferenceLoopHandling = ReferenceLoopHandling.Ignore })
        : "[]";

    var quizProgressData = Model != null
        ? JsonConvert.SerializeObject(Model.GroupBy(q => q.QuizDate).Select(g => new { Date = g.Key, Count = g.Count() }).ToList(), new JsonSerializerSettings { ReferenceLoopHandling = ReferenceLoopHandling.Ignore })
        : "[]";
}

<h1 class="text-center my-4">@ViewData["Title"]</h1>

<div class="container">
  

    <!-- Quiz Scores Details Table -->
    <div class="card mt-4">
        <div class="card-header">
            <h3 class="mb-0">Quiz Scores Details</h3>
        </div>
        <div class="card-body">
            <div class="table-responsive">
                <table id="quizScoresTable" class="table table-striped table-hover">
                    <thead class="table-dark">
                        <tr>
                            <th>Date</th>
                            <th>AS Target</th>
                            <th>Agent satisfaction</th>
                            <th>AS %</th>
                            <th>VS Target</th>
                            <th>Visit satisfaction</th>
                            <th>VS %</th>
                            <th>Quiz Target</th>
                            <th>Quiz Online</th>
                            <th>Quiz %</th>
                            <th>RAM Target</th>
                            <th>Fatal Error</th>
                            <th>RAM %</th>
                            <th>No. of Responses</th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (Model != null && Model.Count > 0)
                        {
                            foreach (var quiz in Model)
                            {
                                <tr>
                                    <td>@quiz.QuizDate</td>
                                    <td>@quiz.asTarget</td>
                                    <td>@quiz.AgentSatisfaction</td>
                                    <td>@quiz.asPercentage</td>
                                    <td>@quiz.vsTarget</td>
                                    <td>@quiz.visitSatisfaction</td>
                                    <td>@quiz.vsPercentage</td>
                                    <td>@quiz.QuizTarget</td>
                                    <td>@quiz.QuizOnline</td>
                                    <td>@quiz.QuizPercentage</td>
                                    <td>@quiz.RamTarget</td>
                                    <td>@quiz.FatalError</td>
                                    <td>@quiz.RamPercentage</td>
                                    <td>@quiz.ResponsesCount</td>
                                </tr>
                            }
                        }
                        else
                        {
                            <tr>
                                <td colspan="7" class="text-center">No quiz score data available.</td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

 *@




 @model List<Pk.Com.Jazz.ECP.Models.ManagersScores>
@using Newtonsoft.Json;

@{
    ViewData["Title"] = "Employee Quiz Scores";

    var quizStatusData = Model != null
        ? JsonConvert.SerializeObject(Model.GroupBy(q => q.Status).Select(g => new { Status = g.Key, Count = g.Count() }).ToList(), new JsonSerializerSettings { ReferenceLoopHandling = ReferenceLoopHandling.Ignore })
        : "[]";

    var quizProgressData = Model != null
        ? JsonConvert.SerializeObject(Model.GroupBy(q => q.QuizDate).Select(g => new { Date = g.Key, Count = g.Count() }).ToList(), new JsonSerializerSettings { ReferenceLoopHandling = ReferenceLoopHandling.Ignore })
        : "[]";
}

<h1 class="text-center my-4">@ViewData["Title"]</h1>

<div class="container">
    <!-- Filter Form -->
    <div class="card mb-4">
        <div class="card-header">
            <h3 class="mb-0">Filter Quiz Scores</h3>
        </div>
        <div class="card-body">
            <form method="get" action="@Url.Action("FilterQuizScores")">
                <div class="row">
                    <!-- Month Dropdown -->
                    <div class="col-md-2">
                        <label for="month">Month</label>
                        <select id="month" name="month" class="form-control">
                            <option value="">Select Month</option>
                            <option value="1">January</option>
                            <option value="2">February</option>
                            <option value="3">March</option>
                            <option value="4">April</option>
                            <option value="5">May</option>
                            <option value="6">June</option>
                            <option value="7">July</option>
                            <option value="8">August</option>
                            <option value="9">September</option>
                            <option value="10">October</option>
                            <option value="11">November</option>
                            <option value="12">December</option>
                        </select>
                    </div>

                    <!-- Year Input -->
                    <div class="col-md-2">
                        <label for="year">Year</label>
                        <input type="number" id="year" name="year" class="form-control" placeholder="e.g., 2024" />
                    </div>

                    <!-- Filter Button -->
                    <div class="col-md-2 align-self-end">
                        <button type="submit" class="btn btn-primary">Filter</button>
                    </div>
                </div>
            </form>
        </div>

    </div>

    <!-- Quiz Scores Details Table -->
    <div class="card mt-4">
        <div class="card-header">
            <h3 class="mb-0">Quiz Scores Details</h3>
        </div>
        <div class="card-body">
            <div class="table-responsive">
                <table id="quizScoresTable" class="table table-striped table-hover">
                    <thead class="table-dark">
                        <tr>
                            <th>Date</th>
                            <th>AS Target</th>
                            <th>Agent satisfaction</th>
                            <th>AS %</th>
                            <th>VS Target</th>
                            <th>Visit satisfaction</th>
                            <th>VS %</th>
                            <th>Quiz Target</th>
                            <th>Quiz Online</th>
                            <th>Quiz %</th>
                            <th>RAM Target</th>
                            <th>Fatal Error</th>
                            <th>RAM %</th>
                            <th>No. of Responses</th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (Model != null && Model.Count > 0)
                        {
                            foreach (var quiz in Model)
                            {
                                <tr>
                                    <td>@quiz.QuizDate</td>
                                    <td>@quiz.asTarget</td>
                                    <td>@quiz.AgentSatisfaction</td>
                                    <td>@quiz.asPercentage</td>
                                    <td>@quiz.vsTarget</td>
                                    <td>@quiz.visitSatisfaction</td>
                                    <td>@quiz.vsPercentage</td>
                                    <td>@quiz.QuizTarget</td>
                                    <td>@quiz.QuizOnline</td>
                                    <td>@quiz.QuizPercentage</td>
                                    <td>@quiz.RamTarget</td>
                                    <td>@quiz.FatalError</td>
                                    <td>@quiz.RamPercentage</td>
                                    <td>@quiz.ResponsesCount</td>
                                </tr>
                            }
                        }
                        else
                        {
                            <tr>
                                <td colspan="14" class="text-center">No quiz score data available.</td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
